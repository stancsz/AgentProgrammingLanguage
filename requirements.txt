# Base runtime dependencies for the Agent Programming Language Python package.
# Currently, the runtime uses only the Python standard library. Pin external
# libraries here once non-stdlib dependencies are introduced.

# Development and tooling dependencies (install manually or via `pip install -e .[dev]`)
# lark-parser>=0.12.0
# pytest>=7.0.0
# black>=23.0.0
# ruff>=0.1.0
# fastapi>=0.110.0
# uvicorn[standard]>=0.23.0
litellm>=1.52.0
