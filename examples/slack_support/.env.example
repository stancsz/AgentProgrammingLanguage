# Example environment file for the Slack support agent runner.
# Copy this to `examples/slack_support/.env` or the repo root `.env` and fill real values.
# Slack credentials are expected to come from your MCP Slack server (see https://modelcontextprotocol.io/registry).

SLACK_SUPPORT_API_TOKEN=change-me
OPENAI_API_KEY=sk-your-openai-key
